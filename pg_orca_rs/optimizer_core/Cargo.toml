[package]
name = "optimizer_core"
version = "0.1.0"
edition = "2021"
license = "AGPL-3.0-or-later"

[dependencies]
# Zero external dependencies — pure safe Rust optimizer core

[dev-dependencies]
proptest = "1"
